# job-test 进度交接（给下一个智能体）

> 生成日期：2026-08-23。这份文档的目标读者是接手本项目的 AI 智能体，
> 2 分钟读完即可无损接上进度。所有事实截至 2026-08-23 验证过。

## 0. 一句话现状

项目本地已全部完工（91 用例 / 覆盖率 57% / Web 平台 / CI 配置），
**卡在"推 GitHub"这一步**，需要人类用户完成两个网页操作后即可收尾推送。
另有体验账号、demo 视频两个遗留待办。

## 1. 项目背景

- **定位**：嘉为科技测试开发岗二面作业——蓝鲸 CMDB + JOB 双系统端到端测试平台。
- **作业三件套要求**：业务理解笔记 ✓、pytest 测试脚本 20-30 用例（现有 91）✓、3 分钟 demo 视频 ✗。
- **核心卖点**：不是"把接口都测一遍"，而是回答两个问题——
  JOB 每条链路单独工作正常吗（分层用例）；两个系统连起来数据对得上吗（场景矩阵，按业务 ID / 主机 ID / 动态分组 ID 三个数据契约组织）。
- **项目主人**：小洋，2026 届应届生，零基础转测试开发，本项目的代码他本人不熟（AI 代写），任何讲解都要从零讲起。

## 2. 目录地图

```
projects/job-test/
├── app/                      # 源码（7 个文件）
│   ├── api_client.py         # JOB ESB 接口封装（40+ 接口，Cookie 认证）
│   ├── cmdb_client.py        # CMDB 查询接口封装（ESB Cookie 认证）
│   ├── gen_cases.py          # AI 用例生成（调 LLM，需 API key，覆盖率 0%）
│   ├── job_config.py         # 公共配置（含测试计划 PLANS、环境常量）
│   ├── storage.py            # SQLite 历史留痕（执行记录、探针日志）
│   └── web_app.py            # FastAPI 单文件 Web 平台（内嵌 HTML，零构建）
├── tests/                    # 12 个测试文件（91 用例）
├── tools/                    # 3 个 Playwright 脚本（录 demo 视频用）
├── docs/                     # 业务笔记、API 文档、CMDB 页面访谈、本交接文档
├── data/                     # SQLite 数据库、测试报告（gitignore）
├── reports/                  # pytest-html 报告输出（gitignore）
├── .github/workflows/ci.yml  # GitHub Actions（push 后自动跑测试+生成覆盖率徽章）
├── .gitlab-ci.yml            # 遗留的 GitLab CI（非主战场，可忽略）
├── pytest.ini                # 11 个 marker 注册 + 选项
├── conftest.py               # 根 fixture（log_probe 钩子、中文报告标题）
├── badge.svg                 # 手写覆盖率徽章（57%），CI 首跑后自动覆盖
└── requirements.txt          # pytest / requests / fastapi / pytest-cov 等
```

## 3. 测试体系（本项目最核心的设计）

### 3.1 用例数量真相

91 用例 = **32 passed + 59 skipped**。59 个 skip 全部是环境依赖用例
（需要真实体验账号 / 部署环境），**skip 是刻意设计，不是失败**——
没账号就诚实 skip，绝不造假绿。这是用户明确要求的底线。

### 3.2 marker 分层（pytest.ini 注册 11 个）

| marker | 含义 | 是否依赖环境 |
|---|---|---|
| unit | 纯本地逻辑 | 否 |
| platform | Web 平台层（见 3.4 递归陷阱） | 否 |
| script / fast_exec / plan / cron / account / file | JOB 各模块接口用例 | 是（需账号） |
| cmdb / integration | CMDB 查询、跨系统联调 | 是 |
| boundary | 边界测试 | 部分是 |

### 3.3 测试计划 PLANS（app/web_app.py 第 38 行）

```python
PLANS = {
    'smoke': 'unit and not platform',   # 冒烟：不等账号秒出
    'regression': 'unit or cmdb or integration',
    'job-only': 'script or fast_exec or plan or cron or account or file',
    'e2e': '...',                       # 全量（见 web_app.py）
}
```

Web 平台"一键跑测试"支持按 plan 名执行，借鉴 MeterSphere 测试计划概念。

### 3.4 关键设计决策（另一个 AI 绝对不要动的地方）

1. **platform marker 防递归自引用**：Web 层测试（tests/test_webapp.py）
   里有"跑冒烟计划"的端到端用例，它会 subprocess 再起 pytest。
   若不把 web 层测试从 smoke 计划里排除，会无限递归 spawn 子进程直至超时
   （这是实际踩过 70 秒超时的 bug）。修复方案就是
   `pytestmark = [pytest.mark.unit, pytest.mark.platform]` +
   `PLANS['smoke'] = 'unit and not platform'`。**拆掉任何一半都会复发。**
2. **Web 层测试隔离**：tests/test_webapp.py 用 TestClient +
   monkeypatch `storage.DB_PATH` 指到临时库，绝不碰真实 data/platform.db。
3. **中文测试函数名**：用户偏好，保持即可，不要改成英文。
4. **Web 平台阶段 0 定位**："有脸可用"，FastAPI 单文件内嵌 HTML，
   不引入前端工程，零构建零依赖。

## 4. 数据快照（2026-08-23 实测）

- 全量跑：32 passed + 59 skipped，约 10.5 秒
- 覆盖率 57%：storage 90% / web_app 74% / api_client 55% / cmdb_client 39% / gen_cases 0%
- Git：3 个 commit（4f482c5 → 9d00fbb → 78c5561）+ tag `v1.0.0`，工作区干净，**尚无 remote**
- README 第 11 行徽章有 `{GITHUB_USER}` 占位符，推送前必须替换为真实用户名
- 敏感信息扫描：0 命中（job_config.py 无硬编码凭证，凭证走 job_config_local.py，已 gitignore）

## 5. 怎么跑（Windows PowerShell）

```powershell
cd E:\ReshapingMyself\projects\job-test

# 冒烟（无需账号，10 秒出结果）
& E:\ReshapingMyself\.venv\Scripts\python.exe -m pytest -m "unit and not platform"

# 全量（无账号时 59 个 skip 属正常）
& E:\ReshapingMyself\.venv\Scripts\python.exe -m pytest

# 覆盖率
& E:\ReshapingMyself\.venv\Scripts\python.exe -m pytest --cov=app --cov-report=term-missing

# Web 平台
& E:\ReshapingMyself\.venv\Scripts\python.exe app\web_app.py
# → 浏览器开 http://127.0.0.1:8000
```

注意：项目无独立 .venv，必须用根目录 `E:\ReshapingMyself\.venv`；
PowerShell 调用带路径的可执行文件必须加 `&` 调用符。

## 6. 待办清单（按优先级）

### 6.1 推 GitHub（进行中，一半卡在用户操作）

已完成：LICENSE（MIT）、.github/workflows/ci.yml、requirements 加 pytest-cov、手写 badge.svg、tag v1.0.0。

需要用户做（用户已收到完整指引）：
1. GitHub → Settings → SSH and GPG keys，粘贴公钥
   `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIMWeUJJlr9W3LmlHUiWjdMKQl96j3DPoYwB1xAP/7uVU zhangyuyang-work`
2. 网页建空仓库 `job-test`（不勾 README/.gitignore/License）

AI 拿到用户名后立即执行：
1. README.md 把 `{GITHUB_USER}` 替换为真实用户名
2. commit（含本交接文档）
3. `git remote add origin git@github.com:<用户名>/job-test.git`
4. `git push -u origin master --tags`
5. 验证 `ssh -T git@github.com` 返回 "Hi <用户名>"
6. CI 首次跑完，README 上会出现绿色对勾 + 真实覆盖率徽章

### 6.2 体验账号（依赖外部，时间线不明）

- 旧环境（智云版）维护停服至 9 月，旧问卷作废。
- 新入口：社区版 7.2，问卷 https://wj.qq.com/s2/9531331/519d/
  （用户周六填过旧问卷，需用新链接重填）。
- 账号到手后：复制 job_config.py 为 job_config_local.py 填凭证 →
  跑全量回归（目标是 59 个 skip 变绿）→ 存证。

### 6.3 demo 视频（二面唯一硬缺口）

- 要求 3 分钟，用 tools/ 下 3 个 Playwright 脚本辅助操作蓝鲸界面。
- 动线建议：起 Web 平台 → 跑冒烟 → 展示报告 → 讲分层思路。
- 录制工具可用 Windows 自带录屏或 OBS，无既定方案，可再议。

## 7. 坑与禁忌

1. **不要拆 3.4 第 1 条的 platform 双 marker 机制**（递归超时 bug 会复发）。
2. **不要造假绿**：没环境就 skip，绝不 mock 掉环境断言假装 passed。
3. **不要用系统 python / C 盘**：环境统一 `E:\ReshapingMyself\.venv`；
   用户习惯文件放 E 盘，不用 C 盘。
4. **PowerShell 没有 `&&`**：多命令用 `;` 分隔。
5. **badge.svg 是手写的**：coverage-badge 包与 Python 3.12 不兼容
   （pkg_resources 缺失），本地别试图重装该包生成徽章，等 CI 自动生成。
6. **web_app.py 的模块 docstring 已被用户主动删除**：尊重现状，不要重新加回。
7. **/api/probe 是白名单只读接口**：写操作一律拒绝，这是安全设计，不要放开。
8. **gen_cases.py 依赖 LLM API key**：当前无 key、覆盖率 0%，属展示性功能，别为此纠结。

## 8. 协作须知（小洋的工作偏好）

- 回复要口语化短句、生活化类比，禁用"依次执行""持久化存储"这类模板黑话。
- 技术问题按"原因是什么 → 为什么 → 怎么办"三角度回答。
- 他零基础，任何概念第一次出现都要讲透，不能假设他懂。
- 他喜欢"完美的作品"：门面工程（README 徽章、LICENSE、CI）对他很重要。
- 他可能随时说"别让我确认了，全权放行"——此时继续干活、事后汇报即可。

## 9. 关键文档指针（面试弹药库）

| 文档 | 位置 | 用途 |
|---|---|---|
| JOB 业务理解与测试方案 | docs/2026-08-22-JOB业务理解与测试方案.md | 面试讲方案的主线 |
| 双系统测试策略 | docs/2026-08-22-分开测还是连块测-双系统测试策略.md | 解释分层设计动机 |
| 端到端平台规划 | docs/2026-08-22-端到端测试平台建设规划.md | 平台蓝图与阶段划分 |
| 体验账号指引 | docs/2026-08-22-JOB体验账号申请指引.md | 账号申请步骤（含新链接） |
| API 文档（40 篇） | docs/apidoc/ | 每个 JOB ESB 接口一篇 |
| 面试剧本 | ../../work/study/2026-07-03-面试剧本-项目诱导点与标准回答.md | 二面话术与诱导点 |

## 10. 若用户开口就是"继续"

优先顺序：① 问 GitHub 用户名（若还没给）→ ② 推送收尾 → ③ 提醒重填体验账号问卷 → ④ 讨论 demo 视频录制方案。前三件完成后本项目即收官。
